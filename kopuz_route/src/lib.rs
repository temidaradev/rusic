#[derive(Clone, Copy, PartialEq, Debug)]
pub enum Route {
    Home,
    Search,
    Library,
    Album,
    Artist,
    Playlists,
    Favorites,
    Activity,
    Ytdlp,
    Settings,
    ThemeEditor,
}
